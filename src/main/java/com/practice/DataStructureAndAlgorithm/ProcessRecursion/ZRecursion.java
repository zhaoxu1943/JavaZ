package com.practice.DataStructureAndAlgorithm.ProcessRecursion;

/**
 * 递归知识让解决方案更清晰,并没有性能上的优势
 *  如果使用循环,性能可能更高,如果使用递归,程序更容易理解
 *
 * 写递归注意:
 * 由于递归要自己调用自己,所以很容易出错
 * 进而导致无限循环
 * 所以编写递归时,一定要告诉它,何时停止递归
 * 所以:每个递归函数都有两个部分:
 * 基线条件(base case) 函数不再调用自己
 * 递归条件(recursive case) 指函数调用自己
 * @author zhaoxu
 * @className ZRecursion
 * @projectName JavaConcentration
 * @date 2020/10/26 21:04
 */
public interface ZRecursion {
}
